import styled from "styled-components";

export const ErrorText = styled.p`
  color: var(--dark);
  font-size: 0.75rem;
  line-height: 1rem;
  margin-bottom: 0px;
  margin-right: 0.8rem;
  text-align: right;
  font-weight: 400;
`;
